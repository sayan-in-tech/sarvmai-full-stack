from django.db import models

# Create your models here.

# Optional: Booking model for future extension
# class Booking(models.Model):
#     booking_id = models.IntegerField()
#     seats = models.CharField(max_length=255)
